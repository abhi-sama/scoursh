{
  "openapi": "3.0.3",
  "info": {"title": "fixture", "version": "1.0"},
  "servers": [{"url": "https://spec-declared-host.example.invalid/api/v2"}],
  "paths": {
    "/pets": {
      "get": {
        "summary": "list",
        "parameters": [
          {"name": "limit", "in": "query"},
          {"name": "X-Trace-Id", "in": "header"}
        ]
      },
      "post": {"summary": "create"}
    },
    "/pets/{petId}": {
      "parameters": [{"name": "petId", "in": "path"}],
      "get": {"summary": "read"},
      "delete": {"summary": "remove"}
    },
    "/never-linked-from-anywhere": {"get": {"summary": "unreachable by crawl"}},
    "/orders": {
      "post": {
        "summary": "IMPORT-03: a real API whose ENTIRE surface is in requestBody, mirroring a real Juice Shop endpoint",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {"$ref": "#/components/schemas/Order"}
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Order": {
        "type": "object",
        "properties": {
          "customerReference": {"type": "string", "example": "cust-42"},
          "orderLines": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "productId": {"type": "string", "example": "p1"},
                "quantity": {"type": "number", "example": 2}
              }
            }
          }
        }
      }
    }
  }
}
