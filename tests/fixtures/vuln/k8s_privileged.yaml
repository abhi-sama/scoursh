# tests/fixtures/vuln/k8s_privileged.yaml - true-positive fixture for
# IAC-K8S-PRIVILEGED-01.
apiVersion: v1
kind: Pod
metadata:
  name: privileged-demo
spec:
  containers:
    - name: app
      image: registry.example.com/app:1.4.2
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
        runAsNonRoot: true
      resources:
        limits:
          cpu: "500m"
          memory: "256Mi"
        requests:
          cpu: "250m"
          memory: "128Mi"
