id: fixture-target
base-url: https://app.fixture.invalid/
extra-host: api.fixture.invalid
allow-subdomains: false
notes: Fixture scope target. Used to prove cloud target attribution
  (rules/RULE-FORMAT.md §9.2.2) without contacting anything.

id: fixture-wide
base-url: https://wide.fixture.invalid/
allow-subdomains: true
notes: Exercises the allow-subdomains branch of the attribution map.

id: fixture-authority
base-url: https://user@authority.fixture.invalid/
extra-host: [2001:db8::1]:8443
allow-subdomains: false
notes: Regression fixture for scope_split_authority (lib/findings.sh): a
  userinfo-bearing base-url and a bracketed-IPv6 extra-host, the two shapes
  that a naive first-colon host split mangles.
