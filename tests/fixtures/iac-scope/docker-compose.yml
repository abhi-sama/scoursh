version: "3.8"
services:
  db:
    image: postgres:15
    privileged: true
    ports:
      - "5432:5432"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - DATABASE_PASSWORD=hunter2correcthorse
